import 'package:flutter/material.dart';

class FindMentorPage extends StatelessWidget {
  const FindMentorPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('mentor'),
    );
  }
}
